<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      SMS permissions: required to scan existing bank/UPI SMS on first launch
      and to listen for new ones as they arrive.

      IMPORTANT (read this before you publish anywhere):
      Google Play restricts READ_SMS / RECEIVE_SMS to apps that are the user's
      default SMS/Dialer handler, with very few approved exceptions. That
      restriction applies to Play Store distribution, not to a sideloaded APK
      (installed directly via `adb install` or a file transfer). This build
      is intended for personal/sideload use. If you later want to list this
      on the Play Store, switch the transaction-ingestion approach to the
      Account Aggregator (AA) framework (e.g. via Setu / Finvu / OneMoney)
      or a NotificationListenerService, and drop these two permissions.
    -->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:label="DebtZero AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
